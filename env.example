# NiPyAPI Actions - Environment Configuration
# Copy this file to .env and fill in your values
# cp env.example .env

# =============================================================================
# Git Flow Registry Authentication
# =============================================================================
# Set the appropriate token for your Git provider.
# If both are set, NIFI_REGISTRY_PROVIDER determines which is used.

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Scopes needed: repo (for private repos) or public_repo (for public repos)
GH_REGISTRY_TOKEN=ghp_your_token_here

# GitLab Personal Access Token
# Create at: https://gitlab.com/-/user_settings/personal_access_tokens
# Scopes needed: api, read_repository, write_repository
# Role needed: Developer or higher on the repository
GL_REGISTRY_TOKEN=glpat-your_token_here

# Registry provider: "github" or "gitlab" (auto-detected from available tokens if not set)
# NIFI_REGISTRY_PROVIDER=github

# Repository for flow registry (owner/repo or namespace/repo format)
NIFI_REGISTRY_REPO=your-org/your-flows-repo

# =============================================================================
# NiFi API Connection
# =============================================================================
NIFI_API_ENDPOINT=https://localhost:9447/nifi-api
NIFI_USERNAME=admin
NIFI_PASSWORD=your_password_here
NIFI_VERIFY_SSL=false

# Optional: Suppress SSL warnings in logs
NIPYAPI_SUPPRESS_SSL_WARNINGS=true
